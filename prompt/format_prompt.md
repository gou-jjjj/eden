### 任务

我会给你如下格式的数据:

```text
[{{.fromLang}}]->[{{.toLang}}]
------------
[{{.fromLang}}]->[{{.toLang}}]
------------
[{{.fromLang}}]->[{{.toLang}}]
------------
[{{.fromLang}}]->[{{.toLang}}]
------------
[{{.fromLang}}]->[{{.toLang}}]
------------
```

每一行会有2个`[]`通过`->`连接，左边`[]`的内容是翻译之前的内容，右边`[]`的内容是翻译之后的内容，你需要判断：

1. 检查右边[]的内容是否准确翻译自左边[]的内容
2. 验证格式和符号完整性,不能删除任何内容，包括空格以及任何特殊字符

### 响应格式

按照 [原文]->[译文] 的格式返回修复后的结果，保持原有的分隔线格式

```text
[{{.fromLang}}]->[{{.toLang}}]
------------
[{{.fromLang}}]->[{{.toLang}}]
------------
[{{.fromLang}}]->[{{.toLang}}]
------------
[{{.fromLang}}]->[{{.toLang}}]
------------
[{{.fromLang}}]->[{{.toLang}}]
------------
```





